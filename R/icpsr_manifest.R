#' @title icpsr_manifest
#' @description Changes the missing values to NA that would code to text descriptions in numeric variables
#' @param download_folder is the numeric value in quotes of the download folder study number.
#' @param manifest_file_append is the text to append to the value from the `download_folder` variable. Defaults to '-manifest.txt'
#' @return
#' @export
icpsr_manifest <- function(download_folder, manifest_file_append = "-manifest.txt") {
# reutrn file paths to read in data
# filepaths, File description, has data .tsv, has data .txt, has setup.sas
#



}
